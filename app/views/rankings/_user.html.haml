%tr
  %td= user_counter + 1
  %td= user.name
  %td= user.try(:stat).points
  %td= user.try(:stat).stat.good_count
  %td= user.try(:stat).stat.bad_count
  %td= correctness(user.stat.good_count, user.stat.bad_count)