!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title Angipl
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
    .container
      %ul.nav.nav-pills
        %li= link_to 'Gra', game_path
        - if user_signed_in?
          %li
            = link_to('Wyloguj się', destroy_user_session_path, :method => :delete)
        - else
          %li
            = link_to('Logowanie', new_user_session_path)

        - if user_signed_in?
          %li
            = link_to('Edytuj profil', edit_user_registration_path)
        - else
          %li
            = link_to('Rejestracja', new_user_registration_path)
      .page-header
        %h1 
          = link_to 'Angi.pl', root_path
          %small Nauka słówek online
      - flash.each do |key, value|
        %div{:class => "alert alert-#{key}"}= value
      = yield
