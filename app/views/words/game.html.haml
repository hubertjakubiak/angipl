%br/
.col-md-6.col-xs-12.col-md-offset-3.text-center
  %pre.lead=@first_word.pl
  - @words.each do |word|
    %pre.answer.lead= link_to word.try(:en), check_word_path(en: "#{word.en}", pl: "#{@first_word.pl}"), {:remote => true, 'data-word' => "#{word.en}", :method => :get}
  %p 
    Wybierz prawidłową odpowiedź.
  %p 
    = link_to "Zaloguj się", new_user_session_path
    lub
    = link_to "załóż konto", new_user_registration_path
    aby śledzić swoje statystyki, które pojawią się w tym miejscu.
.col-md-6.col-xs-12.col-md-offset-3.text-center
  - if current_user
    %p= "Dobre odpowiedzi: #{@stats.good_count}"
    %p= "Złe odpowiedzi: #{@stats.bad_count}"
    %p= "Poprawność: #{correctness(@stats.good_count, @stats.bad_count)}"
    %p= "Ogólne statystyki:"
  %p= "Dobre odpowiedzi: #{@all_good_count}"
  %p= "Złe odpowiedzi: #{@all_bad_count}"
  %p= "Poprawność: #{correctness(@all_good_count, @all_bad_count)}"
