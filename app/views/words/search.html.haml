- title "Szukaj"
.row
  .col-md-4
    %h1 Szukaj
    = form_tag search_words_path, :method => 'get' do
      %p
        = text_field_tag :search, params[:search], class: 'form-control'
        %br/
        = submit_tag "Szukaj", :name => nil, class: 'btn btn-primary'
- if params[:search]
  %p 
    = "#{search_words.count} znalezionych wyników."
  - unless search_words.blank?
    = render partial: 'words', locals: {render_words: search_words}